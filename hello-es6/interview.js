function stringToNum(str) {
  if (typeof str !== 'string') {
    return str
  }

  // 实行减拆分
  let subtractArr = str.split('-')

  // 实行加拆分
  for (let i of subtractArr) {
    let addArr = i.split('+')
    for (let item of addArr) {
      // 乘拆分
      let multiplicationArr =
    }
  }
  let addArr = subtractArr[]

}